package com.lepus;

public class MyRobot {

    public static void main(String[] args) {
        try {
            Thread.sleep(3000);
        }catch (Exception e){
            e.printStackTrace();
        }
        int len = 3;
        int curr = 1;
        for(int i=curr; i<=len; i++){
            // choose character
            // move to grandi
            // choose map
            // clear map1, move to map2
            // clear map2, move to map3
            // clear map3, move to map4
            // clear map4, move to map5
            // clear map5, move to map6
            // clear map6, move to map7
            // clear map7, move to map8
            // clear map8, move to map9
            // clear map9
            // check tired, yes f10, no f12 and esc and home
        }
    }

}